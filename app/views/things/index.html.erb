<p id="notice"><%= notice %></p>

<h1>Art</h1>

<div class="art">
<table>
  <thead>
    <tr>
      <th>Name</th>
      <!--<th>Description</th> -->
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @things.each do |thing| %>

      <span>
        <tr class="art2">
          <td><%= thing.name %></td>
          <!--<td><%= thing.description %></td> -->
          <a><td><%= image_tag thing.image.url(:thumb) %></td></a>

          <span>
            <td><%= link_to 'Show', thing %></td>
            <td><%= link_to 'Edit', edit_thing_path(thing) %></td>
            <td><%= link_to 'Destroy', thing, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </span>
        </tr>
      </span>

    <% end %>
  </tbody>
</table>
</div>

<br>

<%= link_to 'Upload new art', new_thing_path %>
